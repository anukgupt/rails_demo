class MainController < ApplicationController #for functionality of rails
    def index ##rails looks for index.html.erb file inside views in about folder    
        # if(session[:user_id])
        #     #@user=User.find(session[:user_id]) #find is used when we are sure we will find this is
        #     @user=User.find_by(id: session[:user_id])
        #end
    end
end